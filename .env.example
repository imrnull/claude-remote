# claude-remote configuration
# Copy this file to .env and set your values

# Root directory containing all repositories
ROOT_DIR=/path/to/repos

# Default branch to create feature branches from
DEFAULT_SOURCE_BRANCH=main

# Base directory containing Obsidian vaults (for obsidian-claude)
OBSIDIAN_VAULTS_DIR=/path/to/obsidian/vaults
